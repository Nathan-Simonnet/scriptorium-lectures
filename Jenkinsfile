pipeline {
    agent any

    stages {

        stage('Hello') {
            steps {
                echo 'Jenkins fonctionne'
                sh 'pwd'
                sh 'ls -la'
            }
        }

        // stage('Checkout') {
        //     steps {
        //         checkout scm
        //     }
        // }

        // stage('Test') {
        //     steps {
        //         sh 'echo "Tests à définir"'
        //     }
        // }

        // stage('Build') {
        //     steps {
        //         sh 'echo "Build à définir"'
        //     }
        // }
    }
}